logging:
  config: classpath:config/logback.xml
server:
  port: 8888
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:./config/basis_portal